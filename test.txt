xxx-windows
yyy-windows

windows1
winws2
window改了这里的代码
还有这里的hhh
macOS2
mac 改动了这里的代码
win3
冲突后mac的dev分支对此进行进一步修改
win4

wiiiin256

    l = log --stat
    pl = pull --rebase
    cin = commit --amend --no-edit
    ll = log --graph --all --pretty=format:'%Cred%h %Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative

win增加了这里的代码
